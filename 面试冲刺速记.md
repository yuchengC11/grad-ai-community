# 毕业生 AI 互助社区 · 面试冲刺速记（明天面试版）

> 用法：今晚先把第一、三、四部分的**流程**对着代码走两遍，再背其他问答模板。
> 每题答案按口语写，直接念即可；▶ 是追问预案。

---

## 〇、今晚应急策略（先看）

1. **只保三条主线**，能从请求进来到返回讲清楚：
   - 登录鉴权：登录发 JWT → 拦截器验 token → ThreadLocal 取 userId → 用完清理
   - 点赞：写 Redis（Lua 原子切换）→ 脏集合标记 → 定时任务落库 → Redis 挂了降级 MySQL
   - AI：OkHttp 调百炼 → Resilience4j 限流熔断 → 独立线程池/SSE 方案
2. **每个亮点按"问题 → 方案 → 效果"讲**，不要堆名词。
3. **不会的别硬撑**："这个我了解原理，项目里没深入用，我理解是……"比编一个被连环追问戳穿强。
4. **三个绝对不能说错的项目真相**（见第九部分），说错一个容易全盘崩。
5. 数字记牢：点赞定时 **5 分钟**、评论数 **5 秒**、限流 **每秒 5 次**、熔断失败率 **50%/30 秒**、线程池 **10/50/100**、JWT **7 天**。

---

## 一、项目介绍（3 分钟定稿）

> "我做的是一个面向应届毕业生的经验互助社区，前后端分离，后端 Spring Boot + MyBatis-Plus + MySQL + Redis，前端 Vue 3，共 9 张表、二十多个接口，我独立完成。学生可以发求职、考研、考公的经验帖，进行评论、点赞、收藏、加好友、私信；还接入了阿里云百炼的通义大模型，做了多角色 AI 助手、帖子摘要和 AI 评论。
>
> 两个核心设计：
>
> **第一是高并发点赞。** 直接更新数据库会让热帖计数行成为锁竞争热点。我让写操作全走 Redis：用 Set 存每个帖子的点赞用户，天然去重，还能同时数总数、判断用户有没有赞过；'判断+增删'用 Lua 脚本保证原子性；另用一个 Set 标记有变动的帖子，定时任务每 5 分钟只把这些帖子全量同步回 MySQL，起到削峰作用。Redis 故障时自动降级直写 MySQL，靠联合唯一索引和幂等处理兜底，整体是准实时的最终一致。
>
> **第二是 AI 调用的稳定性。** 大模型响应慢又是外部服务，我做了四层保护：OkHttp 分级超时、独立线程池做资源隔离、Resilience4j 每秒 5 次的限流和失败率 50% 熔断 30 秒、故障统一降级成 503；并基于 SseEmitter 实现了 SSE 逐块推送的流式方案。
>
> 基础能力上，JWT + 拦截器 + ThreadLocal 做鉴权，BCrypt 加盐存密码，全站统一响应体和全局异常分级处理。"

▶ 追问地图（往你熟的地方引）：Lua 为什么原子 / 5 分钟窗口丢数据吗 / 熔断三状态 / SSE 和 WebSocket / @Async 自调用 / ThreadLocal 为什么 remove。

---

## 二、登录鉴权（JWT）

### Q：登录鉴权整体流程？
答：注册登录接口在拦截器白名单里。登录时按用户名查库，用 BCrypt 比对密码，通过后用 jjwt 生成 JWT，payload 里只放 userId，HS256 签名、7 天有效，连同用户信息返回（密码置 null）。之后每个请求在 Authorization 头带 token，拦截器验签、验过期，解析出 userId 放进 ThreadLocal，业务层直接取用，请求结束在 afterCompletion 里清理。

### Q：JWT 结构？
答：三段，Header.Payload.Signature。Header 写算法 HS256，Payload 放 userId、签发时间、过期时间，前两段只是 Base64 编码不是加密，Signature 是用服务器密钥对前两段算 HMAC-SHA256，篡改任何内容签名都对不上。服务端只验签不存储，所以无状态、方便水平扩展。
▶ 追问"怎么注销/踢人"：JWT 签发后到期前一直有效，这是先天弱点；改进是 Redis 存黑名单（登出存 jti，TTL 等于剩余有效期），或短 access token + refresh token。**项目目前没做黑名单，别主动说做了。**

### Q：为什么密码用 BCrypt 不用 MD5？
答：MD5 算得快、不带盐，彩虹表和暴力破解成本低；BCrypt 每次 gensalt 生成随机盐拼进密文（$2a$10$ 开头，10 是 cost，2 的 10 次方轮哈希），两个人密码相同密文也不同，计算慢且 cost 可调，盐就在密文里不用单独存。项目用的是 Hutool 封装的 BCrypt。

### Q：为什么用 ThreadLocal？用完为什么 remove？
答：一次请求从拦截器到 Mapper 都在同一条 Tomcat 线程上，ThreadLocal 让这条线程上任意一层都能取到 userId，不用层层传参，且线程间天然隔离。必须 remove 有两个原因：一是 Tomcat 复用线程，不清理下一个请求会拿到上一个用户的 id，造成串号越权；二是线程长期存活，value 一直被强引用无法回收造成内存泄漏。项目在 preHandle 开头和 afterCompletion 各清一次。
▶ 追问"异步线程里能拿到吗"：不能，ThreadLocal 不跨线程池；项目 SSE 方法靠把数据当参数显式传进去规避，通用方案是阿里的 TransmittableThreadLocal。

### Q：拦截器和 Filter 的区别？
答：Filter 是 Servlet 规范，在 DispatcherServlet 之前执行，拦截所有请求包括静态资源，拿不到要执行的 Controller 方法；拦截器是 Spring MVC 的，在 Controller 方法前后执行，能拿到 HandlerMethod，本身是 Spring Bean 可以注入 JwtUtil 这些组件。顺序是 Filter → DispatcherServlet → 拦截器 preHandle → Controller → afterCompletion。项目鉴权放拦截器。
▶ 加分：拦截器发生在 Controller 之前，它返回的 401 走不到 @RestControllerAdvice，所以项目里是拦截器自己用 ObjectMapper 把 JSON 写进响应体。

---

## 三、点赞 / Redis（最高频，务必能默画流程）

### 数据放哪（先背这个）
- Redis 两个 Set：`post:like:{帖子id}` 成员是点赞的 userId，如 {"1","7","9"}；`post:like:sync` 成员是"有变动待落库"的帖子 id。
- MySQL 两张：`post_like` 明细表（post_id+user_id 联合唯一索引 uk_post_user）；`sys_post.like_count` 计数字段。
- 分工：Redis 扛在线读写，MySQL 当持久底稿。

### Q：点赞为什么用 Redis 不用 MySQL？
答：高并发下大家都更新热帖的同一行计数，InnoDB 行锁让更新串行排队，数据库扛不住。Redis 是内存操作没有行锁，写请求全部打 Redis，再由定时任务异步批量落库，把瞬时高峰摊平成每 5 分钟一次批量写，这就是削峰。

### Q：一次点赞请求的完整流程？
答：先过登录拦截器拿到 userId；Controller 只转发，PostServiceImpl.toggleLike 里先 getById 校验帖子存在；然后调 RedisUtil.toggleSetAtomic 执行 Lua，返回 1 是点赞、0 是取消；成功后把帖子 id 加进 post:like:sync 脏集合，返回结果。整个在线过程不写 MySQL。

### Q：Lua 脚本为什么能保证原子性？
答：Redis 命令执行是单线程的，Lua 脚本会被当成一个整体在服务器端一口气执行完，中间不会插入其他客户端的命令。我要做的是"先 SISMEMBER 判断在不在，再决定 SADD 还是 SREM"两步逻辑，用 Java 分两次调 Redis 中间会被并发插入导致状态错乱，所以必须用 Lua。

### Q：为什么用 Set 不用 String 计数器？
答：Set 一个结构同时解决三件事：SCARD 数总数、SISMEMBER 判断当前用户赞没赞过（决定红心状态）、成员天然去重。纯数字计数器回答不了"我赞过没"。

### Q：定时落库怎么做的？
答：LikeSyncTask 用 @Scheduled cron 每 5 分钟跑：先 SMEMBERS 拿脏集合，空的就结束；对每个帖子，SMEMBERS 拿完整点赞用户列表，然后**全量覆盖**——先 deleteByPostId 删光旧明细，再逐条 insert，最后 UPDATE sys_post 把 like_count 设成集合大小（赋绝对值，不是 +1）；成功才把帖子从脏集合移除，失败不移除、下轮重试。方法上有 @Transactional 保证整批原子。
▶ 追问"为什么全量删了重插"：不用算增量 diff，逻辑简单不会漏，重复执行结果一样，天然幂等。
▶ 追问"为什么用 Set 做标记"：同一帖连点 100 次集合里只有一个 id，只同步一次，避免扫全表。

### Q：Redis 挂了怎么办？
答：写路径 Lua 执行异常就降级走 toggleLikeByMySQL：查明细表，有记录就删除并把计数减一，没有就插入并加一；并发下重复插入会被唯一索引拦下抛 DuplicateKeyException，catch 后幂等返回成功。读路径 SCARD、SISMEMBER 异常就改查 MySQL。
▶ **诚实点**：点赞读降级只查库返回、没回填 Redis（评论数冷启动才有回填）；Redis 重启丢数据后的重建是空白，改进是开 AOF、启动时从 MySQL 预热、加定时对账任务。

### Q：缓存和数据库一致性？
答：准实时的最终一致。用户读 Redis 立刻看到最新结果，MySQL 在 5 分钟内被刷平；社区点赞能接受分钟级不一致，这是方案前提。对比：收藏是低频操作，项目用 Lua 切换后同请求内同步双写 MySQL；评论明细实时入库，只有评论数走 Redis（INCR，每 5 秒落库）。

### Q：Redis 还用过哪些数据类型？
答：String 做评论数计数器（INCR 原子自增）；Set 做点赞集合和同步标记。其他类型了解：Hash 存对象、List 做简单队列、ZSet 做排行榜，项目里没用到。

---

## 四、AI 模块

### Q：AI 能力整体怎么接的？
答：没有引官方 SDK，用 OkHttp 手动调阿里云百炼的 OpenAI 兼容接口 /chat/completions，Jackson 拼和解析 JSON，模型 qwen3.7-plus。三个功能：POST /ai/chat 多角色对话（非流式）、GET /ai/post/summary 帖子摘要、GET /ai/post/genComment AI 评论，后两个是后端拼好标题正文的 Prompt 调同一个底层方法。

### Q：SSE 原理？和 WebSocket 区别？
答：SSE 是服务器到浏览器的**单向**推送，基于 HTTP、text/event-stream 格式，浏览器 EventSource 自带断线重连，只传文本，适合 AI 打字机、通知这种单向场景；WebSocket 是全双工长连接，握手后升级成 ws 协议，双向收发、支持二进制，适合聊天室、协同编辑，但重连心跳都要自己做。
实现上 Spring 用 SseEmitter：Controller 立即返回 emitter，Servlet 容器把响应挂起、释放 Tomcat 线程，异步线程每拿到一个片段就 emitter.send，结束发 [DONE] 再 complete。
▶ **项目真相（按这个口径说）**：后端 SSE 链路已实现并自测（SseEmitter 120 秒超时 + 逐行解析 data: 取 choices[0].delta.content），当前版本前端走非流式接口拿全文、用前端动画做打字机效果，接通只差一个返回 SseEmitter 的 Controller 端点。**非流式取 message.content，流式取 delta.content。**

### Q：为什么 AI 调用要用独立线程池？
答：大模型响应要十几到几十秒，如果占着 Tomcat 的请求线程（默认 200 个）死等，几十个并发 AI 请求就能把线程占满，登录发帖等正常接口也拿不到线程，一个慢服务拖垮全站，这叫线程饥饿。独立 aiExecutor 线程池做舱壁隔离：核心 10、最大 50、队列 100，拒绝策略 CallerRunsPolicy（池满让调用线程自己跑，形成反压而不是丢任务），并配置了优雅停机最多等 60 秒。

### Q：@Async 自调用为什么失效？怎么解决？
答：@Async 靠 Spring AOP 动态代理实现，容器注入的是代理对象，外部调用经过代理切面才会丢到线程池；类内部 this 调用的是原始对象、绕过代理，注解静默失效。项目在 AiPostServiceImpl 里用 @Lazy 自注入一个自身代理 selfProxy，通过 selfProxy 调 doStreamChat；@Lazy 是为了打破自己依赖自己的循环依赖。

### Q：熔断器原理和三状态？参数说一下。
答：CLOSED 闭合正常放行；滑动窗口统计最近 10 次调用、样本至少 5 次后，失败率达到 50% 就 OPEN 打开，30 秒内所有请求直接走 fallback 不发 HTTP；30 秒后自动转 HALF_OPEN 半开，放 2 个试探请求，成功就闭合恢复，失败再打开 30 秒。限流是每秒 5 个许可、超时等待 0 即拿不到立刻拒绝，既匹配大模型 QPS 配额，也防刷接口烧钱。只把 AiException、IOException、TimeoutException 计入失败率，代码 bug 不算服务故障。
▶ fallback 真相：它不是返回友好文案，而是把异常统一归一化成 AiException 抛出，最后由全局异常处理器变成 503。

### Q：超时怎么控制？
答：OkHttp 四级：connectTimeout 5 秒（建立连接）、writeTimeout 10 秒（发送请求）、readTimeout 60 秒（读数据，大模型生成慢所以最长）、callTimeout 90 秒（整个调用总上限）。

### Q：怎么防用户刷 AI 接口？
答：第一所有 AI 接口都要登录，拦截器兜底；第二 Resilience4j 全局限流每秒 5 次；第三入参校验，问题最长 2000 字、AI 评论指令 500 字、历史最多 20 轮；第四请求体 max_tokens 限 1500 控制每次生成成本。可改进点是按用户维度限流（现在是全局）。

### Q：怎么保证 AI 输出质量？
答：强格式 system prompt，强制按"核心建议/详细分析/行动清单/注意事项"四段输出并限制字数；persona 参数切换学长、面试官、导师等角色；temperature 设 0.3 让建议类回答更稳定；返回后做 cleanResponse 清洗异常符号和残缺 Markdown，前端 marked 渲染再过 DOMPurify 防 XSS。

---

## 五、并发 / JVM

### Q：ThreadLocal 原理和内存泄漏？
答：每个 Thread 对象里有一个 ThreadLocalMap，key 是 ThreadLocal 对象（弱引用），value 是我们 set 的值（强引用）。key 被 GC 后变成 null 键，但 value 还在，线程如果是线程池里长期不死的，value 就一直泄漏；所以用完必须 remove。项目靠拦截器统一清理。

### Q：线程池参数和流程？怎么定的？
答：流程：核心线程满了进队列，队列满了扩到最大线程，再满触发拒绝策略。参数核心 10、最大 50、队列 100、CallerRunsPolicy。AI 调用是 IO 密集型（大部分时间在等网络），线程数可以大于 CPU 核数，所以给得比 CPU 密集型大；CallerRunsPolicy 不丢任务，让调用方自己执行来降低消费速度，是温和的反压。

### Q：悲观锁和乐观锁？（你项目里怎么体现）
答：悲观锁假设一定冲突，操作前先加锁，比如 synchronized、SELECT ... FOR UPDATE；乐观锁假设不常冲突，更新时用版本号或 CAS 校验，失败重试。**我项目没有在热点行上加锁，而是用架构避开竞争**：点赞的并发判断交给 Redis 单线程 + Lua 原子完成，MySQL 唯一索引做最后防线，定时任务单线程串行跑且计数用绝对值覆盖，天然不存在并发加错的问题。如果非要在 DB 层做，可以用乐观锁版本号或一条原子 UPDATE 语句。

### Q：项目哪里用了事务？
答：点赞定时落库（删旧明细、插新明细、更新计数要一起成功）；发评论（插评论和更新计数）；好友申请的接受/拒绝/删除；私信发送和标记已读。都写了 @Transactional(rollbackFor = Exception.class)，因为 Spring 默认只对 RuntimeException 回滚，加上 Exception 后受检异常也回滚。
▶ 追问失效场景：自调用、异常被 catch 吞掉、方法非 public、数据库引擎不支持事务。

---

## 六、数据库

### Q：表结构怎么设计的？
答：9 张表：sys_user 用户、sys_post 帖子、category 分类、comment 评论、post_like 点赞明细、sys_favorite 收藏、friend 好友关系、private_message 私信、sys_message 系统消息。表之间用逻辑外键（存 id，不建物理外键，方便分表和性能）。
点赞表唯一索引 uk_post_user(post_id, user_id) 是数据库最后防线，保证一人一帖最多一条，重复插入直接报错，代码据此做幂等；username 也有唯一索引防重复注册。

### Q：分页怎么做的？
答：MyBatis-Plus 的 Page 对象配合 PaginationInnerInterceptor 分页插件（配 MySQL 方言），自动拼 LIMIT 并查 total，是物理分页，不会把全表加载进内存。
▶ 深分页：LIMIT 100000,10 会扫描前 10 万行，优化用游标（where id > 上次最大 id）或子查询先定位主键。

### Q：慢查询怎么优化？你项目有 N+1 吗？
答：项目列表页要显示作者昵称头像，我在 fillUserInfo 里先把当页帖子的 userId 去重收集，用 listByIds 一条 IN 批量查回，再在内存里用 Map 组装，避免在循环里逐条查用户（那就是 N+1）；收藏状态也是批量 IN 查询，列表还直接用 LEFT JOIN ... CASE WHEN 一次带出 isFavorited。通用手段：where、order by 字段建索引（idx_post_id、idx_user_id、idx_create_time），EXPLAIN 看执行计划。
▶ **诚实短板（被问不足时说）**：私信会话列表 getChatList 对每个会话循环查最后一条消息和未读数，存在 N+1，可以改成一条 GROUP BY 子查询或 MyBatis 的 collection 关联映射。

### Q：事务隔离级别？
答：四个：读未提交、读已提交（Oracle 默认）、可重复读（MySQL InnoDB 默认）、串行化；分别解决脏读、不可重复读、幻读。InnoDB 在可重复读下靠 MVCC 快照读加间隙锁解决幻读。我项目用默认的可重复读，定时任务"先删后插"在一个事务里，其他事务在提交前看不到删了一半的中间状态，保证落库结果要么全成要么全不成。

---

## 七、Spring / Java 基础

### Q：IoC 和 AOP 在项目哪里体现？
答：IoC 是对象交给 Spring 容器创建和注入，Controller、Service、Mapper、工具类全是 @RestController/@Service/@Component，我用 @Resource、@Autowired、@Value 取用，不自己 new 对象，好处是解耦、方便替换和测试。AOP 是不改动业务代码做增强，项目里 @Async、@Transactional、Resilience4j 的 @CircuitBreaker/@RateLimiter 都是靠动态代理切面实现的；拦截器也是类似的横切增强思想（严格说是 SpringMVC 的责任链机制）。

### Q：Bean 生命周期？@PostConstruct 哪用了？
答：实例化 → 属性注入 → 初始化（@PostConstruct）→ 投入使用 → 容器关闭时销毁。JwtUtil 里用 @PostConstruct 写 init：等 jwt.secret 注入完成后校验非空（空就直接让启动失败），不足 32 字节就循环补足到 32 字节（HS256 要求 256 位密钥）。不能写在构造器里，因为构造器执行时 @Value 还没注入。

### Q：@Resource 和 @Autowired 区别？
答：@Autowired 是 Spring 提供的，默认按类型注入；@Resource 是 JDK 规范（JSR-250），默认按名称注入。@Value 用来注入配置文件的值，比如 jwt.secret、upload.local-path。更推荐构造器注入：字段可 final、能暴露循环依赖、单测好 new。

### Q：统一响应和全局异常怎么做的？
答：所有接口返回 Result<T>，结构是 code、msg、data，成功 200，参数错 400，未登录 401，AI 异常 503，未知异常 500。用 @RestControllerAdvice + @ExceptionHandler 按异常类型就近匹配：参数校验 BindException、类型错误、缺参数返回 400，自定义 AiException 返回 503，最后 Exception 兜底 500，兜底时日志打完整堆栈、给前端只返回友好提示。前端只判断 code 一个字段。

---

## 八、缓存三大问题速成（必背，尽量往项目上靠）

| 问题 | 含义 | 通用解法 | 你项目的情况 |
|---|---|---|---|
| 穿透 | 查一个**数据库里本来就没有**的数据，缓存没有、每次都打到库 | 缓存空值（短 TTL）；布隆过滤器 | 查不存在的帖子直接返回 404，可加空值缓存 |
| 击穿 | **一个热点 key 过期瞬间**，大量请求同时打到库 | 互斥锁重建（只放一个请求查库回填）；逻辑过期 | 点赞集合 key **不设过期时间**，没有这个问题 |
| 雪崩 | **大量 key 同时过期**或 Redis 整体宕机 | TTL 加随机值；Redis 主从/集群；服务降级 | Redis 宕机有 MySQL 降级兜底；生产应加集群和 AOF |

答题套路：先解释现象 → 说通用解法 → 补一句"我项目里点赞 key 是持久的不涉及过期问题，Redis 整体故障用降级兜底，真要加固我会加 AOF 持久化、主从和启动预热"。

---

## 九、防翻车清单（项目真相，说错会被连环追问）

1. **SSE**：后端实现了但没有 Controller 暴露端点，线上是普通 POST + 前端 setInterval 模拟打字。口径："实现了方案，当前版本前端用非流式，接通只差一个端点。"
2. **JWT 黑名单/登出**：没实现，RedisUtil 只有通用操作。被问就讲改进方案，别说做了。
3. **PostLikeServiceImpl 是死代码**（旧版直写 MySQL，没人注入），问起来说"早期实现，已被 PostServiceImpl 的 Redis 方案替代"。
4. **点赞读降级没有回填 Redis**，评论数冷启动才有回填。
5. **OkHttp 超时是代码硬编码**（5/10/60/90 秒），properties 里的超时配置没生效。
6. **私信不是实时的**：没有 WebSocket、没有轮询，手动刷新；被问改进就说可升级 WebSocket 或 SSE 未读数推送。
7. **backup.sql 已过时**（旧表名 post，代码是 sys_post，还缺好几张表），以实体类为准；别主动提。
8. **熔断 fallback 是抛 AiException**，不是直接返回提示文案。
9. 限流是**全局**每秒 5 次，不是按用户；被问改进就说按 userId 维度做 key。
10. 定时任务**没有分布式锁**，多实例会重复执行；幂等设计保证结果正确，改进用 ShedLock。

---

## 十、"项目有什么不足 / 你怎么改进"（送分题，背 3 条）

1. Redis 故障恢复后的缓存重建没做：加 AOF + 启动从 MySQL 预热 + 定时 SCARD 与明细表对账。
2. SSE 流式端点接通，前端 EventSource 替换轮询式动画；私信升级 WebSocket。
3. 定时任务加 ShedLock 分布式锁支持多实例；限流细化到用户维度；私信会话列表消除 N+1。

## 反问面试官（选 2 个）
- 团队目前后端的技术栈和服务规模是怎样的？
- 新人入职后会先接触哪块业务，有没有人带？
- 团队代码评审和发布流程大概是什么样的？
- 这个岗位接下来最希望新人先补齐哪方面能力？

---

**心态**：你是应届生，面试官预期是"基础扎实、项目能讲清原理、不会的诚实"。三条主线讲顺、数字说对、不足敢承认，就已经超过很多背书的候选人。今晚别再铺新东西，把三条链路各默画一遍，早点休息。

---

# 项目二：tlias 员工管理系统（Spring Boot + MyBatis + PageHelper）

> ⚠️ 代码真相：当前 day10 版本只有"分页条件查询"和"批量删除"，**没有修改员工的方法**。
> `<set>` 动态更新是课程后续内容，按下面的标准写法背；面试官翻代码时如实说"这是第一阶段代码，修改功能写法是 `<set>+<if>`"。

## Q：MyBatis 动态标签怎么实现"按需更新"？逗号问题怎么处理？

**做了什么**（标准写法，要能默写）：

```xml
<update id="update">
    update emp
    <set>
        <if test="name != null and name != ''">name = #{name},</if>
        <if test="gender != null">gender = #{gender},</if>
        <if test="entrydate != null">entrydate = #{entrydate},</if>
        <if test="deptId != null">dept_id = #{deptId},</if>
        <if test="updateTime != null">update_time = #{updateTime}</if>
    </set>
    where id = #{id}
</update>
```

**为什么这么做**：编辑页面用户可能只改了一两个字段，没改的字段前端不传（就是 null）。如果写全字段 update，没传的字段会被覆盖成 null。用 `<if>` 判断"非空才拼进 SQL"，实现传什么改什么。

**逗号问题**：每个片段后面手写了逗号，但最后一个生效的片段后面不该有逗号，SQL 语法会错。`<set>` 标签自动做两件事：补上 SET 关键字、**把末尾多余的逗号删掉**。它等价于：

```xml
<trim prefix="SET" suffixOverrides=","> ... </trim>
```

**项目里真实存在的同类写法**：条件查询用 `<where>` + `<if>`，`<where>` 会自动去掉开头多余的 AND（所以第一个 if 不写 and，后面都写 and）：

```xml
<where>
    <if test="name != null and name != ''">name like concat('%',#{name},'%')</if>
    <if test="gender != null">and gender = #{gender}</if>
    <if test="begin != null and end != null">and entrydate between #{begin} and #{end}</if>
</where>
```

▶ 追问 `#{}` 和 `${}`：`#{}` 是预编译占位符 ?，参数后设置，防 SQL 注入；`${}` 是直接字符串拼接，有注入风险，只在要拼表名、order by 字段时用。

## Q：PageHelper 分页原理？（按执行流程背）

1. Controller 接收 page（默认1）、pageSize（默认10）和姓名、性别、入职日期范围等条件；
2. Service 第一行调 `PageHelper.startPage(page, pageSize)`——**这一步把分页参数存进 ThreadLocal，绑在当前线程上**；
3. 紧接着执行 `empMapper.list(...)` 条件查询（★ startPage 和查询之间不能插别的查询，否则分页参数会被错误消费，且只对紧邻的下一条查询生效）；
4. PageHelper 本质是 MyBatis 插件（拦截 Executor.query），它从 ThreadLocal 取参数后做两件事：
   - 自动执行一条 count 查询（SELECT COUNT(*) FROM (原查询)）得到总记录数；
   - 按 MySQL 方言改写原 SQL，拼上 `LIMIT (page-1)*pageSize, pageSize`；
5. 返回的 List 实际是 PageHelper 的 Page 对象（继承 ArrayList，内部带 total），强转后取 getTotal()、getResult()；
6. 封装成 PageBean{total 总条数, rows 当前页列表} 返回，分页参数用完从 ThreadLocal 清掉。

▶ 对比社区项目：MyBatis-Plus 的 PaginationInnerInterceptor 原理相同（拦截 SQL 加 LIMIT + 查 count），区别是 MP 把页码放在 Page 对象里传递，不用 ThreadLocal。

## Q：`<foreach>` 批量删除为什么比循环单条删除快？

代码：

```xml
<delete id="delete">
    delete from emp where id in
    <foreach collection="ids" item="id" separator="," open="(" close=")">
        #{id}
    </foreach>
</delete>
```

Controller 是 `@DeleteMapping("/{ids}")` + `@PathVariable List<Integer> ids`，前端请求 `/emps/1,2,3`，Spring 按逗号切成列表。最终生成一条 SQL：`delete from emp where id in ( ?, ?, ?)`。

快的原因：
- 循环单条删：N 条记录 = N 次**网络往返**，每次都要占用连接、发送、数据库解析 SQL、执行、事务提交；
- foreach：**一条 SQL、一次网络往返、数据库只解析优化一次、一个事务提交**，数据量越大差距越明显；
- 每个值仍是 `#{}` 占位符，预编译参数，防注入。

▶ 注意：in 列表不能无限大（SQL 过长、参数过多会超限），超大批量要分批，每批比如 500~1000 条。

## 项目二优化点（加分）
- 批量删除配合逻辑删除（deleted 字段 + @TableLogic）替代物理删除，保留数据可追溯；
- 分页深页优化：LIMIT 大偏移慢，改游标查询；
- 分页插件统一配置方言，count 查询可自定义优化（left join 不需要的表去掉）。

---

# 项目一补充两道高频追问

## Q：Redis 恢复后，缓存和数据库不一致怎么办？
答：要分场景。第一种，Redis 宕机期间写请求降级走了 MySQL，数据在库里，但 Redis 重启后内存是空的，用户再读会显示 0——这是我项目目前的短板。我的改进方案有三层：一是开启 AOF 持久化让重启不丢；二是做冷启动预热，应用或 Redis 启动时把近期热点帖子的点赞明细从 MySQL 全量加载回 Redis；三是加定时对账任务，比对 Redis 的 SCARD 和 MySQL 的计数，不一致就以 MySQL 为权威数据源重建缓存（删 key 再批量 SADD）。读路径降级查 MySQL 后也应该顺手回填（评论数冷启动已有这个逻辑，点赞读路径还没加）。

## Q：定时任务重复执行（或多实例部署）怎么处理？
答：单实例下 Spring 的 @Scheduled 默认串行调度，同一任务不会并发。多实例时每台机器都会跑，我靠两层保证不出错：第一是幂等设计，落库是"先全删再重插 + 计数赋绝对值 + 成功才移除脏标记"，重复执行结果完全一样；第二如果上集群，我会加分布式锁，比如 Redis 的 SET NX 或 ShedLock，保证同一时刻只有一个实例执行。落库失败时脏标记不移除，下一轮继续重试，也是安全的。
